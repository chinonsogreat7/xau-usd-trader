# Market data

Keep local data under this directory. Raw and processed market data are ignored by Git.

The first supported CSV format is documented in `docs/DATA_FORMAT.md`. Never commit broker
credentials, account exports, or licensed market data unless its licence explicitly permits it.
